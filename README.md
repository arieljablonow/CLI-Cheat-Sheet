# CLI Cheat Sheet

## Directory Traversal

`cd` [directory] 
naviate to 'directory'

`cd -` 
go back one level 

`cd ~`
go to home directory 

`cd ..`
go back one directory

`pwd`
get current file path
